[run]
branch = True
source = djangocms_background_media

[report]
omit = 
    *migrations*
    *tests*
    *__init__*
    *apps*
    *static*

exclude_lines =
    # Don't complain about missing debug-only code:
    def __repr__
    if self\.debug

ignore_errors = True
